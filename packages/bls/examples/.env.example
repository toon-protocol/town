# @toon-protocol/bls — Docker Compose environment variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# ---------------------
# Required Variables
# ---------------------

# Unique node identifier
NODE_ID=my-node

# 64-character lowercase hex Nostr secret key
# Generate one with: openssl rand -hex 32
NOSTR_SECRET_KEY=

# Node's ILP address (must start with "g." — alphanumeric, dots, hyphens only)
ILP_ADDRESS=g.toon.my-node

# ---------------------
# Optional Variables
# ---------------------

# HTTP port (default: 3100, range: 1–65535)
# BLS_PORT=3100

# Base price per byte for event storage (default: 10)
# BLS_BASE_PRICE_PER_BYTE=10

# 64-char hex pubkey — events signed by this key bypass payment
# OWNER_PUBKEY=

# Directory for SQLite database storage (default: /data)
# DATA_DIR=/data

# JSON object mapping event kinds to custom prices
# Example: {"0":"0","30023":"100"}
# BLS_KIND_OVERRIDES=

# ---------------------
# Pricing Variable Aliases
# ---------------------
# The following aliases are also accepted (fallback chain):
#   BLS_BASE_PRICE_PER_BYTE > RELAY_BASE_PRICE_PER_BYTE > BASE_PRICE_PER_BYTE
#   BLS_KIND_OVERRIDES > RELAY_KIND_OVERRIDES > KIND_OVERRIDES
