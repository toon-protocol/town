{
  "name": "@toon-protocol/rig",
  "version": "0.1.0",
  "description": "Forge-UI: Decentralized git forge web interface for TOON Protocol",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@toon-format/toon": "^1.0.0"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
