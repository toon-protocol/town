schema: 1
story: '9.6'
story_title: 'Publish to npm'
gate: PASS
status_reason: 'All 6 acceptance criteria verified. Three packages published to npm at v1.0.0 in correct dependency order, fresh project install and TypeScript compilation confirmed, git tag v1.0.0 created.'
reviewer: 'Quinn (Test Architect)'
updated: '2026-02-11T18:30:00Z'

waiver: { active: false }

top_issues: []

risk_summary:
  totals: { critical: 0, high: 0, medium: 0, low: 0 }
  recommendations:
    must_fix: []
    monitor:
      - '13 transitive dependency vulnerabilities (upstream: @ardrive/turbo-sdk, @cosmjs/*, elliptic) — monitor for upstream patches'

quality_score: 100
expires: '2026-02-25T18:30:00Z'

evidence:
  tests_reviewed: 0
  risks_identified: 0
  trace:
    ac_covered: [1, 2, 3, 4, 5, 6]
    ac_gaps: []

nfr_validation:
  security:
    status: PASS
    notes: 'No secrets in tarballs; files field restricts to dist/ only; npm auth token not committed'
  performance:
    status: PASS
    notes: 'No runtime code changes; publish-only story'
  reliability:
    status: PASS
    notes: 'Post-publish verification confirmed install and TypeScript compilation in fresh project'
  maintainability:
    status: PASS
    notes: 'Thorough documentation in story file; publish order and rollback strategy documented'

recommendations:
  immediate: []
  future:
    - action: 'Add a LICENSE file to the repository root so published tarballs include it'
      refs: ['packages/bls/', 'packages/core/', 'packages/relay/']
    - action: 'Monitor upstream dependency vulnerabilities for patches (elliptic, @cosmjs/*, @ardrive/turbo-sdk)'
      refs: ['packages/core/package.json', 'packages/relay/package.json']
    - action: 'Fix pre-existing subscribeMany test failures'
      refs:
        [
          'packages/core/src/discovery/SocialPeerDiscovery.test.ts',
          'packages/core/src/bootstrap/RelayMonitor.test.ts',
        ]
